change the way of displaying draggable nodes in the properties panel to make room for all nodes
add all the built-in python functions to the list of available functions
make a parent svelte component for pure functions so I don't have to repeat the code for each function
bring back the live preview when running the flow but in a better way
add an outliner that shows all the nodes in the flow with their ids
add a way to select a node in the outliner and have it selected in the flow
add a way to select a node in the flow and have it selected in the outliner
add a right click menu for adding nodes
remove the nodes section in the My Flow sidebar